# 测试/开发依赖(生产镜像不装,见 .dockerignore 排除 tests/)
#
# 为什么要锁版本: CI 里 `pip install pytest` 装的是最新版。pytest 大版本更新可能
# 改变收集规则 —— 最坏情况不是报错,而是**静默少收测试**(绿灯但实际没跑),
# 那就等于没有测试门禁。升级走"改这里 + 本地跑一遍 tests/"。
pytest==9.1.1
